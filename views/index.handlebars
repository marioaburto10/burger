<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="top-bar">
      <img id="burger-image" src="/assets/images/burger.png" alt="burger" />
      <h1>Eat-A-Burger!</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-6 text-center">

      <h2>Burgers that have not been eaten!</h2>

      <div class="row">

          {{#each burgers}}
            {{#unless this.devoured}}
              <div class="col-xs-9 text-center">
              
                <pre>{{this.id}}. {{this.burger_name}}</pre>
              </div>

              <div class="col-xs-3 text-center">
                <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                    <input type="hidden" name="devoured" value="1">
                    <button class="btn pull-right" type="submit">EAT BURGER!</button>
                </form>
              </div>
              
            {{/unless}}
          {{/each}}
      </div>
      
    </div>

    <div class="col-xs-6 text-center">

      <h2>Burgers that have been eaten!</h2>

      <div class="row">

        {{#each burgers}}
          {{#if this.devoured}}
            <div class="col-xs-9 text-center">
              <pre>{{this.id}}. {{this.burger_name}}</pre>
            </div>

            <div class="col-xs-3">
              <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="0">
                <button class="btn pull-right" type="submit">UN-EAT BURGER</button>
              </form>
            </div>
            
          {{/if}}
        {{/each}}
      </div>
      
    </div>
  </div>

  <div class="row">
    <div class="col-xs-6">

      <h2>Add a burger</h2>

      <form class="create-update-form" action="burgers/create" method="POST">

        <div class="form-group">
          <label for="bg">Burger Name:</label>
          <input type="text" id="brg" name="burgerInput">
        </div>

        <button class="btn " type="submit">ADD BURGER</button>
      </form>
    </div>

    <div class="col-xs-6">

      <h2>Delete a burger</h2>
      <form class="delete-form" action="burgers/delete?_method=DELETE" method="POST">

        <div class="form-group">
          <label for="brg">ID of the burger you want to delete:</label>
          <input type="text" id="brg" name="delete">
        </div>

        <button class="btn " type="submit">DELETE BURGER</button>
      </form>
    </div>
  </div>
  
</div>